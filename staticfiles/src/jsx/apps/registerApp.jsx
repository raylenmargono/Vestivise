import React from 'react';
import ReactDOM from 'react-dom';
import RegisterView from '../authComponents/registerView.jsx';

const root = document.getElementById('registerApp');

ReactDOM.render(
	<RegisterView/>
, root);