import React from 'react';
import ReactDOM from 'react-dom';
import LoginView from '../authComponents/loginView.jsx';

const root = document.getElementById('loginApp');

ReactDOM.render(
	<LoginView/>
, root);